# inter

### Сборка проекта
Для компиляции используйте `clang++` (или `g++`):
```bash
clang++ -g main.cpp read_binary.cpp read_file.cpp write_file.cpp -o main
```

### Запуск проекта
```bash
./main
```
